print(not True) # NOT operador de negação, padrão de programação

# "-" operador unario negativo, retornam o valor da operacao
num_negativo = -22
num_positivo = 22

print(-num_positivo)
print(-num_negativo) # dois "-" vira positivo
print(-2553251.2)

# não existe incremento no python, no estilo do C por exemplo (variavel++) (variavel--)